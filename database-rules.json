{
  "rules": {
    "projects": {
      "$projectId": {
        "users": {
          "$userId": {
            ".read": "auth != null && auth.uid == $userId",
            ".write": "auth != null && auth.uid == $userId",
            "planner": {
              "$date": {
                ".read": "auth != null && auth.uid == $userId",
                ".write": "auth != null && auth.uid == $userId"
              }
            },
            "habits": {
              "$date": {
                ".read": "auth != null && auth.uid == $userId",
                ".write": "auth != null && auth.uid == $userId"
              }
            },
            "expenses": {
              "$date": {
                ".read": "auth != null && auth.uid == $userId",
                ".write": "auth != null && auth.uid == $userId"
              }
            },
            "work": {
              "$date": {
                ".read": "auth != null && auth.uid == $userId",
                ".write": "auth != null && auth.uid == $userId"
              }
            },
            "pomodoro": {
              "$date": {
                ".read": "auth != null && auth.uid == $userId",
                ".write": "auth != null && auth.uid == $userId"
              }
            },
            "workout": {
              "workoutDay": {
                ".read": "auth != null && auth.uid == $userId",
                ".write": "auth != null && auth.uid == $userId"
              },
              "_meta": {
                ".read": "auth != null && auth.uid == $userId",
                ".write": "auth != null && auth.uid == $userId"
              },
              "$year": {
                "$month": {
                  "$day": {
                    ".read": "auth != null && auth.uid == $userId",
                    ".write": "auth != null && auth.uid == $userId"
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
